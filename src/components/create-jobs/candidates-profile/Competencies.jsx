import Text from "../../Text.jsx";
import names from "../create-jobs-names.jsx";

const Competencies = () => {
  return (
    <div className="white-card">
      <Text className="title">{names.competencies.name}</Text>
    </div>
  );
};

export default Competencies;
